name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: |
        cd C:\\Users\\User\\Documents\\GitHub\\info408
        npm install

    - name: Build application
      run: |
        cd C:\\Users\\User\\Documents\\GitHub\\info408
        npm run build

    - name: Run tests
      run: |
        cd C:\\Users\\User\\Documents\\GitHub\\info408
        npm test
