name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Copy files
        run: |
          mkdir -p deployment
          cp index.html deployment/
          cp script.js deployment/

          
